import type { History } from '@/lib/types'

export async function fetchHistory(city: string): Promise<History[]> {
  // TODO: Replace with actual MCP history service call.
  throw new Error('History MCP service not implemented yet')
}
